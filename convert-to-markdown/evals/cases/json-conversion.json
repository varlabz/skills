{
  "id": "json-conversion",
  "name": "JSON to Markdown conversion",
  "prompt": "Convert this JSON file at ./config/settings.json to markdown.",
  "expected_behavior": [
    "Use 'uvx markitdown' for JSON files",
    "Output structured content in readable format"
  ],
  "success_criteria": [
    "Uses correct tool: 'uvx markitdown'",
    "Produces human-readable output (not raw JSON dump)",
    "Handles nested structures reasonably well"
  ],
  "edge_cases": [
    "Very large JSON files → may produce unwieldy output",
    "JSON with binary data → cannot be meaningfully converted to text",
    "Malformed JSON → markitdown may fail or produce partial output"
  ]
}
